; This personal project uses the public npm registry.
; The global ~/.npmrc points at a work CodeArtifact registry, which does not
; host these packages (and its token expires). This override is scoped to this
; folder only and does not change your global npm config.
registry=https://registry.npmjs.org/
